[{
  "name": "db_get_users",
  "description": "Fetch users with optional filters.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {"type": ["string","null"]},
      "name": {"type": ["string","null"]},
      "email": {"type": ["string","null"]},
      "status": {
        "type": ["string", "null"],
        "enum": ["ACTIVE", "INACTIVE"],
        "description": "User status filter - ACTIVE, INACTIVE or null for all"
      },
      "createdBefore": {"type": ["string","null"]},
      "createdAfter": {"type": ["string","null"]},
      "updatedBefore": {"type": ["string","null"]},
      "updatedAfter": {"type": ["string","null"]},
      "businessUnit": {
        "type": ["string", "null"],
        "enum": ["engineering", "sales", "marketing", "hr", "finance", null],
        "description": "Business unit filter"
      }
    },
    "required": []
  },
  "execution": {
    "type": "http",
    "method": "GET",
    "url": "https://scout-shqtd6.5sc6y6-4.usa-e2.cloudhub.io/users",
    "headers": { },
    "query_map": {
      "id": "id",
      "name": "name",
      "email": "email",
      "status": "status",
      "createdBefore": "createdBefore",
      "createdAfter": "createdAfter",
      "updatedBefore": "updatedBefore",
      "updatedAfter": "updatedAfter",
      "businessUnit": "businessUnit"
    },
    "timeout": 30
  }
}, {
  "name": "db_get_payments",
  "description": "Fetch payment records with optional filters.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": ["array", "null"],
        "items": {"type": "integer"},
        "description": "List of payment IDs to filter by"
      },
      "userId": {
        "type": ["array", "null"], 
        "items": {"type": "integer"},
        "description": "List of user IDs to filter payments by"
      },
      "status": {
        "type": ["string", "null"],
        "description": "Payment status filter"
      },
      "dateFrom": {
        "type": ["string", "null"],
        "description": "Start date for payment filtering (YYYY-MM-DD)"
      },
      "dateTo": {
        "type": ["string", "null"],
        "description": "End date for payment filtering (YYYY-MM-DD)"
      }
    },
    "required": []
  },
  "execution": {
    "type": "http",
    "method": "POST",
    "url": "https://scout-shqtd6.5sc6y6-4.usa-e2.cloudhub.io/payments",
    "headers": {
      "Content-Type": "application/json"
    },
    "body_map": {
      "id": "id",
      "userId": "userId",
      "status": "status",
      "dateFrom": "dateFrom",
      "dateTo": "dateTo"
    },
    "timeout": 30
  }
}]
