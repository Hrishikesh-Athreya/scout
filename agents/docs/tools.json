[{
  "name": "create_document",
  "description": "Create a document using a template and document values.",
  "parameters": {
    "type": "object",
    "properties": {
      "template": {
        "type": "string",
        "description": "Template name to use for document creation (e.g., 'template1', 'template2')"
      },
      "documentValues": {
        "type": "object",
        "description": "Complete document data with all required fields"
      }
    },
    "required": ["template", "documentValues"]
  },
  "execution": {
    "type": "http",
    "method": "POST",
    "url": "http://localhost:8081/document/create",
    "headers": {
      "Content-Type": "application/json"
    },
    "body_map": {
      "template": "template",
      "documentValues": "documentValues"
    },
    "timeout": 30
  }
}]
