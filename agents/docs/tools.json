[{
  "name": "generate_document_report",
  "description": "Generate a document/report using template1 or template2 with provided content.",
  "parameters": {
    "type": "object",
    "properties": {
      "template": {
        "type": "string",
        "enum": ["template1", "template2"],
        "description": "Template to use - template1 (3 Q&As) or template2 (4 Q&As)"
      },
      "documentValues": {
        "type": "object",
        "description": "Complete document content with all fields"
      },
      "enablePasswordProtection": {
        "type": "boolean",
        "description": "Whether to enable password protection for the document",
        "default": false
      }
    },
    "required": ["template", "documentValues"]
  },
  "execution": {
    "type": "http",
    "method": "POST",
    "url": "https://scout-shqtd6.5sc6y6-4.usa-e2.cloudhub.io/document/generate",
    "headers": {
      "Content-Type": "application/json"
    },
    "body_map": {
      "template": "template",
      "documentValues": "documentValues",
      "enablePasswordProtection": "enablePasswordProtection"
    },
    "timeout": 60
  }
}]
